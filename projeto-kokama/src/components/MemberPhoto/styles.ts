import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  text: {
    fontSize: 15,
  },
  container: {
    display: "flex",
    alignItems: "center",
    height: 100,
    margin: 8,
  },
  photo: {
    maxHeight: 80,
    maxWidth: 80,
    borderRadius: 250,
  },
});

export default styles;
